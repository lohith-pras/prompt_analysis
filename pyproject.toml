[project]
name = "prompt-dashboard"
version = "0.1.0"
description = "Generate HTML dashboards from exported Claude conversation logs"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
